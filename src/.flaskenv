FLASK_APP=main:setup # searches for a file called main.py instead of app.py as well as a function called setup
FLASK_DEBUG=1
FLASK_RUN_PORT=8080

# can also put the DB_URI and JWT_KEY however it is not secure. 